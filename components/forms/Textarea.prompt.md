One-sentence: Labeled multi-line text input for messages and longer fields.

```jsx
<Textarea label="Project description" rows={4} hint="A sentence or two is plenty." />
```

Props: `label`, `required`, `hint`. Vertically resizable; forwards native `<textarea>` attributes.
